index=windows EventCode=4625

| bucket span=5m _time

| stats count
        values(host) as TargetHost
        by Source_Network_Address
           Account_Name
           _time

| where count>=10

| rename Source_Network_Address as SourceIP

| table _time
        SourceIP
        Account_Name
        TargetHost
        count

| sort -count
